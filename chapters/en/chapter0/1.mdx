# Introduction[[introduction]]

Welcome to the comprehensive Hugging Face 101 Course! This course is your gateway to understanding and leveraging the power of the Hugging Face ecosystem - the world's largest collaborative platform for machine learning that has revolutionized how we build, share, and deploy AI models.

Whether you prefer working through web interfaces, building with APIs, or diving deep into code, this course offers multiple pathways to master the Hugging Face ecosystem. You can explore models through the Hub's web interface, integrate AI capabilities via APIs, or build custom applications with Python - or combine all three approaches!

## What You'll Learn

This course will take you on a journey through the entire Hugging Face ecosystem, from basic concepts to advanced applications. You'll discover how to:

- **Navigate the Hugging Face Hub**: Understand the platform that hosts over 500,000 models, 100,000+ datasets, and 300,000+ interactive applications
- **Use Inference Providers**: Learn to leverage multiple AI providers through a unified API for text generation, image creation, and more
- **Share Your Work**: Master the art of sharing models, datasets, and applications with the global AI community
- **Build Interactive Demos**: Create engaging applications using Gradio and deploy them as Spaces
- **Integrate Advanced Protocols**: Explore cutting-edge technologies like the Model Context Protocol (MCP) for enhanced AI capabilities
- **Evaluate Models**: Learn to evaluate AI models for real-world applications.

## Why This Course Matters

The Hugging Face Hub represents more than just a repository - it's the central nervous system of the global AI community. It's where all the models, datasets, and applications are shared and built. Moreover, with the recent introduction of the [Model Context Protocol (MCP)](https://huggingface.co/blog/building-hf-mcp), AI models can now seamlessly connect with external data sources, tools, and environments, dramatically expanding what's possible in AI applications.

Whether you're a curious beginner taking your first steps into AI, a professional developer looking to integrate cutting-edge models into applications, or a researcher wanting to share groundbreaking work with the world, this course provides the knowledge and practical skills you need.

## Learning Pathways Through This Course

This course is designed to accommodate different learning styles and technical backgrounds. You can choose your own adventure through three main pathways:

### 🖥️ **UI-First Pathway**: Start with Web Interfaces
Perfect for beginners or those who prefer visual interfaces:
- Explore models and datasets through the Hugging Face Hub web interface
- Use Inference Endpoints directly in your browser
- Create and share Spaces without writing code
- Build Gradio demos through the web interface
- **Chapters to focus on**: 1, 3, 4, parts of 6

### 🔌 **CLI-First Pathway**: Integration Through CLI
Ideal for developers who want to integrate AI into existing applications:
- Use the Inference API for quick model testing
- Integrate Hugging Face models via REST APIs
- Deploy models with Inference Endpoints
- **Chapters to focus on**: 1, 2, 5, 7

### 🐍 **Code-First Pathway**: Deep Dive with Python Client
For those who want full control and customization:
- Install and use Hugging Face libraries locally
- Fine-tune models and create custom datasets
- Build advanced Gradio applications
- **Chapters to focus on**: All chapters with hands-on coding

### 🌟 **Hybrid Pathway**: Mix and Match
Combine approaches as needed for your projects - start with the UI, then add API calls, and dive into code when you need customization.

## Getting Started

Your setup depends on which pathway you choose:

**For All Pathways:**
- A [Hugging Face account](https://huggingface.co/join) (free)
- Curiosity about AI and machine learning

**Additional for API and Code Pathways:**
- Basic understanding of web APIs (for API pathway)
- Python knowledge (helpful for code pathway, but we'll guide you through it)

<Tip>**UI-First Learners**: You can skip the technical setup below and jump straight to Chapter 1 to start exploring the Hugging Face Hub through your web browser!</Tip>

<Tip>**API and Code Learners**: Ensure your Python environment is up-to-date to avoid compatibility issues during installation. If something fails to install, restart your runtime and try again.</Tip>

**Note**: While this course covers setup for Linux and macOS systems, Windows users can follow along using Google Colab notebooks, which work seamlessly across all platforms.

## Using a Google Colab notebook[[using-a-google-colab-notebook]]

*This section is for the **Code-First** and **API-First** pathways. UI-First learners can skip to Chapter 1.*

Using a Colab notebook is the simplest possible setup; boot up a notebook in your browser and get straight to coding! 

If you're not familiar with Colab, we recommend you start by following the [introduction](https://colab.research.google.com/notebooks/intro.ipynb). Colab allows you to use some accelerating hardware, like GPUs or TPUs, and it is free for smaller workloads.

Once you're comfortable moving around in Colab, create a new notebook and get started with the setup:

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter0/new_colab.png" alt="An empty colab notebook" width="80%"/>
</div>

The next step is to install the libraries that we'll be using in this course. We'll use `pip` for the installation, which is the package manager for Python. In notebooks, you can run system commands by preceding them with the `!` character.

For this course, you'll need several key Hugging Face libraries:

```python
# Core Hugging Face libraries
!pip install huggingface_hub
!pip install transformers
!pip install datasets
!pip install gradio

# Optional: For advanced features and MCP integration
!pip install "huggingface_hub[inference]"
```

You can verify the installation by importing the main libraries:

```python
import huggingface_hub
import transformers
import datasets
import gradio

print("✅ All Hugging Face libraries installed successfully!")
```

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter0/install.gif" alt="A gif showing the result of the installation commands" width="80%"/>
</div>

### What Each Library Does

- **`huggingface_hub`**: The central library for interacting with the Hugging Face Hub, including authentication, model/dataset management, and the new MCP client capabilities
- **`transformers`**: Provides thousands of pre-trained models for natural language processing, computer vision, and audio tasks
- **`datasets`**: Offers easy access to thousands of datasets and efficient data processing tools
- **`gradio`**: Enables you to build and share interactive machine learning demos with just a few lines of code

For users interested in the cutting-edge [Model Context Protocol (MCP)](https://huggingface.co/docs/huggingface_hub/en/package_reference/mcp) integration, the inference extras provide additional capabilities for connecting AI models with external tools and data sources.

This installation will give you access to the complete Hugging Face ecosystem and prepare you for all the exciting projects in this course!

## Using a Python virtual environment[[using-a-python-virtual-environment]]

*This section is for advanced users following the **Code-First** pathway who prefer local development.*

If you prefer to use a Python virtual environment, the first step is to install Python on your system. We recommend following [this guide](https://realpython.com/installing-python/) to get started.

Once you have Python installed, you should be able to run Python commands in your terminal. You can start by running the following command to ensure that it is correctly installed before proceeding to the next steps: `python --version`. This should print out the Python version now available on your system.

When running a Python command in your terminal, such as `python --version`, you should think of the program running your command as the "main" Python on your system. We recommend keeping this main installation free of any packages, and using it to create separate environments for each application you work on — this way, each application can have its own dependencies and packages, and you won't need to worry about potential compatibility issues with other applications.

In Python this is done with [*virtual environments*](https://docs.python.org/3/tutorial/venv.html), which are self-contained directory trees that each contain a Python installation with a particular Python version alongside all the packages the application needs. Creating such a virtual environment can be done with a number of different tools, but we'll use the official Python package for that purpose, which is called [`venv`](https://docs.python.org/3/library/venv.html#module-venv).

First, create the directory you'd like your application to live in — for example, you might want to make a new directory called *transformers-course* at the root of your home directory:

```
mkdir ~/transformers-course
cd ~/transformers-course
```

From inside this directory, create a virtual environment using the Python `venv` module:

```
python -m venv .env
```

You should now have a directory called *.env* in your otherwise empty folder:

```
ls -a
```

```out
.      ..    .env
```

You can jump in and out of your virtual environment with the `activate` and `deactivate` scripts:

```
# Activate the virtual environment
source .env/bin/activate

# Deactivate the virtual environment
deactivate
```

You can make sure that the environment is activated by running the `which python` command: if it points to the virtual environment, then you have successfully activated it!

```
which python
```

```out
/home/<user>/transformers-course/.env/bin/python
```

### Installing dependencies[[installing-dependencies]]

As in the previous section on using Google Colab, you'll now need to install the Hugging Face libraries. Install the complete ecosystem with:

```bash
# Core libraries
pip install huggingface_hub transformers datasets gradio

# Optional: Advanced features including MCP support
pip install "huggingface_hub[inference]"
```

Verify your installation:

```python
python -c "import huggingface_hub, transformers, datasets, gradio; print('✅ Ready for the Hugging Face 101 Course!')"
```

## Authentication Setup

Before starting the course, authenticate with the Hugging Face Hub to access all features:

```python
from huggingface_hub import login
login()
```

This will prompt you for your Hugging Face token, which you can find in your [account settings](https://huggingface.co/settings/tokens).

You're now all set up and ready to explore the amazing world of Hugging Face! 🚀

## What's Next?

Regardless of which pathway you choose, we recommend starting with **Chapter 1: The Hugging Face Hub** to understand the ecosystem. From there:

- **UI-First learners**: Continue with the web interface exploration
- **API-First learners**: Jump to Chapter 2 to start making API calls
- **Code-First learners**: Follow the chapters sequentially for the complete hands-on experience
- **Hybrid learners**: Pick and choose based on your current project needs

The beauty of the Hugging Face ecosystem is that these approaches complement each other - you can start with the UI, prototype with APIs, and then implement with code as your needs evolve.
